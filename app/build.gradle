apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'Apps'
            keyPassword 'blender999'
            storeFile file('C:/Users/Jamal/Dropbox/Keystore/keystore.jks')
            storePassword 'blender999'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId "com.jmrandombitz.librarygenesis"
        minSdkVersion 21
        targetSdkVersion 21
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:22.0.0'
    compile 'org.jsoup:jsoup:1.8.1'
}
